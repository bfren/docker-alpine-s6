#!/bin/sh

set -euo pipefail


#======================================================================================================================
# Echo debug message in grey if DEBUG flag is set - otherwise do nothing.
#
# Arguments
#   1   Text to echo
#======================================================================================================================

[[ "${DEBUG-}" = "1" ]] && bcg-e "bcg" "\e[1;30m" "${1-}"
