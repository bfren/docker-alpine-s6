#!/usr/bin/nu-env

use s6

# Terminate the container
def main [
    --executable (-e): string   # Set to override BF_E before calling termination script
] {
    if $executable != null { s6 env BF_E $executable }
    s6 cont terminate
}
