#!/usr/bin/nu

use bf
use bf-s6
bf env load -e

# Terminate the container
def main [
    executable?: string # Override BF_E with this
] {
    if $executable != null { bf env set_executable $executable }
    bf-s6 cont terminate
}
