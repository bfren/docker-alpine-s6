#!/command/with-contenv nu

use s6

# Terminate the container
def main [...args] {
    # allow caller to override BF_E
    if ($args | length) == 1 {
        s6 env E $args.0
    }

    s6 cont terminate
}
