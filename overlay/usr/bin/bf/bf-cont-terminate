#!/usr/bin/nu

use bf
use bf-s6
bf env load -x

# Terminate the container
def main [
    executable?: string # Override BF_X with this
] {
    if $executable != null { bf env x_set $executable }
    bf-s6 cont terminate
}
