#!/usr/bin/with-contenv sh

set -euo pipefail


#======================================================================================================================
# Forward cron error log to Docker
#======================================================================================================================

FILE=/var/log/cron.log && touch ${FILE}
bcg-forward "cron-log" ${FILE}
